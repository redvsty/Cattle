---
# vars file for LEMP
install_packages:
          - make
          - wget
          - epel-release
          - gcc
          - gcc-c++
          - pcre-devel
          - expat-devel
          - libxml2-devel
          - libcurl-devel
          - libvpx-devel
          - libjpeg-devel
          - libpng-devel
          - libXpm-devel
          - freetype-devel
          - libmcrypt
          - libmcrypt-devel
          - bzip2-devel
          - gmp-devel
          - readline-devel
          - libxslt-devel
          - libzip.x86_64
          - libzip-devel.x86_64
          - openssl-devel
          - libicu.x86_64
          - libicu-devel.x86_64
          - screen
          - libtool-ltdl-devel
          - autoconf
          - bison
          - bison-devel
          - re2c
          - libsqlite3x-devel.x86_64
          - oniguruma-devel.x86_64
          - oniguruma-devel

MODULES: --enable-xml --enable-opcache   -with-jpeg --with-curl --with-freetype --with-xpm --with-openssl --enable-calendar --with-libdir=lib64 --with-config-file-path=/usr/local/php{{ FS_MOUNTPOINT2 }}/etc --enable-fpm --enable-bcmath --with-bz2 --enable-ctype --enable-dom --enable-exif --with-gettext --with-gmp --with-mhash --with-iconv --enable-phar --with-readline --enable-shmop --enable-simplexml --enable-sockets --enable-tokenizer --enable-xmlreader --enable-xmlwriter --with-xsl --enable-fileinfo --enable-mbstring --enable-mysqlnd --with-mysqli --with-pdo-mysql --with-pdo-sqlite --with-pear --with-pecl --enable-posix --enable-session --with-sqlite3 --with-zlib --enable-filter --enable-pcntl --enable-sysvmsg --enable-sysvsem --enable-sysvshm --enable-intl --enable-soap --with-zip --enable-gd